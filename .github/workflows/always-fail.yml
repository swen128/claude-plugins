name: Always Fail (test ci-watcher)

on:
  pull_request:
  push:
    branches: [main]

jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - name: Fail on purpose
        run: |
          echo "This job intentionally fails to test ci-watcher-channel."
          exit 1
